"use strict"; const formatargito = t => `0${t}`.slice(-2), atualizar = (t, e) => { let { segundos: a, minutos: o, horas: s, dias: i } = e; if (a && o && s && i) { let n = t % 60, r = Math.floor(t % 3600 / 60), l = Math.floor(t % 86400 / 3600), d = Math.floor(t / 86400); a.textContent = formatarDigito(n), o.textContent = formatarDigito(r), s.textContent = formatarDigito(l), i.textContent = formatarDigito(d) } else console.error("Um ou mais elementos dos contadores n\xe3o foram encontrados.") }, contagemRegressiva = (t, e) => { let a = () => clearInterval(s), o = () => { t <= 0 ? (atualizar(0, e), a()) : (atualizar(t, e), t--) }; o(); let s = setInterval(o, 1e3) }, tempoRestante = t => { let e = Date.now(); return Math.floor((t - e) / 1e3) }, initContador = () => { let t = new Date("2024-09-18T07:00:00"), e = { segundos: document.getElementById("segundos"), minutos: document.getElementById("minutos"), horas: document.getElementById("horas"), dias: document.getElementById("dias") }; e.segundos && e.minutos && e.horas && e.dias ? contagemRegressiva(tempoRestante(t), e) : console.error("Um ou mais elementos dos contadores n\xe3o foram encontrados.") }, dAnimate = (t, e, a, o = !1) => { if (!t) { console.error("Elemento n\xe3o encontrado para anima\xe7\xe3o."); return } let s = o ? "flex" : "block"; switch (e) { case "show": t.style.cssText = `display: ${s}; transition: opacity .4s; opacity: 0;`, setTimeout(() => { t.style.cssText += "opacity: 1;" }, a); break; case "hide": t.style.cssText = `display: ${s}; transition: opacity .4s; opacity: 0;`, setTimeout(() => { t.style.cssText += "display:none;" }, a) } }, activeElement = t => { for (let e of t) if (e.classList.contains("active")) return e; return null }; $(document).ready(() => { $("a").on("click", function (t) { if ("" !== this.hash) { t.preventDefault(); let e = this.hash; $("html, body").animate({ scrollTop: $(e).offset().top }, 800, () => { window.location.hash = e }) } }) }), ScrollReveal().reveal(".home__data, .home__img, .about__data, .about__img, .services__content, .shirt__data, .shirt__img, .guide, .guide__container, .contentText  .map__data, .mapimg, .footer__content", { origin: "left", opacity: .7, distance: "100px", duration: 1500, reset: !1, interval: 20 }), document.addEventListener("DOMContentLoaded", initContador); const days = document.querySelectorAll("#guide .days .day"); days.forEach(t => { t.addEventListener("click", e => { if (t !== activeElement(days) && activeElement(days)) { activeElement(days).classList.remove("active"), e.target.classList.add("active"); let a = document.querySelectorAll("#guide .details div"), o = document.getElementById(e.target.dataset.day); if (o && !o.classList.contains("active")) { let s = activeElement(a); s && (dAnimate(s, "hide", 5), s.classList.remove("active")), setTimeout(() => { o && (o.classList.add("active"), dAnimate(o, "show", 200)) }) } } }) });